const WingToken = artifacts.require("WingToken");

module.exports = async function (deployer) {
    // await deployer.deploy(WingToken);
    // const token = await WingToken.deployed();
    // console.log("Token: " + token.address);
};
